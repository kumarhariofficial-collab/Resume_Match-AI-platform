{
  "name": "resume-match-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build:packages": "npm run build --workspace=packages/core-types && npm run build --workspace=packages/resume-parser && npm run build --workspace=packages/jd-analyzer && npm run build --workspace=packages/ats-auditor && npm run build --workspace=packages/matching-engine && npm run build --workspace=packages/ai && npm run build --workspace=packages/export-engine",
    "build": "npx prisma generate --schema=./prisma/schema.prisma && npm run build:packages && npm run build --workspace=apps/web",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "prisma": "^6.3.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
